import chalk from "chalk";

const log = console.log;

log(chalk.yellow.bold('🦀 A new Rust + WebAssembly version is coming soon!'));
log(chalk.yellow('⚡ Expected performance improvements:'));
log(chalk.yellow('   • 2-5x faster execution'));
log(chalk.yellow('   • Lower memory usage'));
log(chalk.yellow('   • Better cross-platform compatibility'));
